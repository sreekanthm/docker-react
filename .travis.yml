sudo: required

language: generic 

services:
  - docker

before_install:
  - docker build -t deeptinair/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true deeptinair/docker-react npm run test